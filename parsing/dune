(library
 (public_name stt.parsing)
 (name parsing)
 (libraries zarith stt menhirLib sedlex)
 (preprocess
  (pps sedlex.ppx)))

(menhir
 (modules tokens)
 (flags --only-tokens))

(menhir
 (modules parser tokens)
 (merge_into parser)
 (flags --table --external-tokens Tokens))
